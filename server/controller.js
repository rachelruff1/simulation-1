const getShelf = (res, req, next) => {
//   const db = req.app.get("db");
};
const getBin = (res, req, next) => {
//   const db = req.app.get("db");
};
const updateBin = (res, req, next) => {
//   const db = req.app.get("db");
};
const deleteObj = (res, req, next) => {
//   const db = req.app.get("db");
};
const createObj = (res, req, next) => {
//   const db = req.app.get("db");
};

module.exports = {
  getShelf,
  getBin,
  updateBin,
  deleteObj,
  createObj
};
